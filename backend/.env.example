# Primary: Cerebras (streaming chat + placeholder). Omit CEREBRAS_API_KEY to use OpenRouter only.
CEREBRAS_API_KEY=
CEREBRAS_MODEL=gpt-oss-120b

# Fallback when Cerebras is unavailable or fails before the first streamed token
OPENROUTER_API_KEY=sk-or-your-key-here
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
APP_URL=http://localhost:7860
